global.__approot = require('path').resolve(__dirname, '../../')

var models = require(__approot + '/models');

describe('API Polls', function () {
    describe('Comments', function () {
        describe('POST', function () {
            xit('should not accept empty texts', function () {

            });
        });
    });
});

